function NotFound() {
  return (
    <div>
      <p>All user data is stored either locally in your browser or on Spotify. Data is transmitted to Spotify and nowhere else.</p>
      <p>Playlist IDs and names and song IDs and names are stored locally.</p>
    </div>
  );
}

export default NotFound;
